Group these patterns by theme. Return a JSON object where keys are group names and values are lists of pattern numbers (1-indexed).

Every pattern must appear in exactly one group. Create 2-5 groups based on natural thematic clusters.

Example output:
{{"groups": {{"engagement timing": [1, 3, 5], "content quality": [2, 4]}}}}

Patterns:
{patterns}

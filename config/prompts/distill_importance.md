Rate each pattern's importance (1-10).
1=trivial observation, 5=useful pattern, 10=critical behavioral rule that changed outcomes.

Return a JSON object: {{"scores": [8, 5, 9, ...]}}
One score per pattern, in the same order.

Patterns:
{patterns}

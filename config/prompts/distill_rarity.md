Rate how rare or novel each NEW pattern is compared to the EXISTING patterns (1-10).
1=very common theme already well-covered, 5=somewhat distinct, 10=completely unique perspective.

A pattern covering a topic already well-represented scores LOW.
A pattern introducing a genuinely new angle scores HIGH.

Respond with ONLY a JSON object:
{{"scores": [8, 3, 9]}}

EXISTING patterns in this category:
{existing_patterns}

NEW patterns to rate:
{new_patterns}

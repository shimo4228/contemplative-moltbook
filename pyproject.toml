[project]
name = "contemplative-agent"
version = "0.2.0"
description = "Contemplative AI agent for Moltbook — promoting alignment through the four axioms"
requires-python = ">=3.9"
license = {text = "MIT"}
dependencies = ["requests>=2.28.0", "numpy>=1.24.0"]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-cov>=4.0", "responses>=0.23.0"]

[project.scripts]
contemplative-agent = "contemplative_agent.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/contemplative_agent"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "unit: Unit tests",
    "integration: Integration tests",
]

[dependency-groups]
dev = []

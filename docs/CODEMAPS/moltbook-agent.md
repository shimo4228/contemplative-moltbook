<!-- Generated: 2026-03-22 | Files scanned: 33 | Token estimate: ~1400 -->
# Moltbook Agent Codemap

## Module Dependency Graph

```
cli.py (585L)  -- composition root
 -> core/  (14 modules)
 |    _io.py (57L)              -- file I/O (write_restricted, truncate, archive_before_write)
 |    config.py (26L)           -- security constants (FORBIDDEN_*, VALID_*, MAX_*)
 |    domain.py (303L)          -- domain config + prompt/rules loader
 |    prompts.py (55L)          -- lazy-loading proxy to config/prompts/*.md (17 templates)
 |    llm.py (367L)             -- Ollama interface, circuit breaker, sanitization
 |    episode_log.py (127L)     -- Layer 1: append-only JSONL episode storage
 |    knowledge_store.py (163L) -- Layer 2: distilled patterns (JSON)
 |    memory.py (443L)          -- Layer 3: MemoryStore facade + agents.json
 |    scheduler.py (165L)       -- rate limit scheduling, persistence
 |    distill.py (259L)         -- 2-stage distill + distill_identity + summarize_record
 |    insight.py (208L)         -- behavior pattern extraction, skill file gen
 |    report.py (228L)          -- activity report generation (JSONL → Markdown)
 |    metrics.py (160L)         -- session metrics aggregation
 |
 -> adapters/moltbook/  (11 modules)
 |    config.py (91L)           -- URLs, paths, timeouts, rate limits
 |    agent.py (653L)           -- session orchestrator (feed/reply/post cycles)
 |    session_context.py (53L)  -- shared session state contract
 |    feed_manager.py (304L)    -- feed fetch, scoring, engagement, dedup
 |    reply_handler.py (384L)   -- notification reply processing (SessionContext)
 |    post_pipeline.py (161L)   -- dynamic post generation (SessionContext)
 |    client.py (446L)          -- HTTP client (auth, domain lock, retry)
 |    auth.py (111L)            -- credential management, register
 |    verification.py (236L)    -- obfuscated math challenge solver
 |    content.py (130L)         -- rules-based content, dedup, axiom intro
 |    llm_functions.py (236L)   -- Moltbook-specific LLM functions
 |
 -> adapters/meditation/  (4 modules)
      config.py (55L)           -- state space definition, parameters
      pomdp.py (294L)           -- JSONL → POMDP matrices (numpy)
      meditate.py (206L)        -- Active Inference cycles
      report.py (146L)          -- result interpretation → KnowledgeStore

config/                         -- externalized templates (domain-swappable)
  domain.json                   -- submolts, thresholds, topic keywords
  knowledge.json                -- learned patterns [{"pattern": "...", "distilled": "YYYY-MM-DD"}]
  identity.md                   -- agent persona (updated by distill-identity)
  prompts/*.md (17 files)       -- LLM prompt templates with {placeholders}
  config/constitution/*.md      -- constitutional clauses
  skills/*.md                   -- behavior patterns (generated by insight)
  history/identity/             -- identity.md archives (timestamped)

~/.config/moltbook/             -- runtime data (env var MOLTBOOK_HOME)
  logs/YYYY-MM-DD.jsonl         -- daily episode log (append-only)
  agents.json                   -- followed agents, last_update timestamp
  rate_state.json               -- request budgets, timestamps
  credentials.json              -- API key + agent_id (0600)
  commented_cache.json          -- post dedup cache
```

**Total: 33 modules, ~6658 LOC, 17 test files, 651 tests**

## Key Classes

| Class | File | LOC | Role |
|-------|------|-----|------|
| `Agent` | adapters/moltbook/agent.py | 653 | Session orchestrator |
| `SessionContext` | adapters/moltbook/session_context.py | 53 | Shared mutable state |
| `ReplyHandler` | adapters/moltbook/reply_handler.py | 384 | Notification replies |
| `PostPipeline` | adapters/moltbook/post_pipeline.py | 161 | Organic post generation |
| `FeedManager` | adapters/moltbook/feed_manager.py | 304 | Feed engagement |
| `MoltbookClient` | adapters/moltbook/client.py | 446 | HTTP client |
| `EpisodeLog` | core/episode_log.py | 127 | Append-only JSONL |
| `KnowledgeStore` | core/knowledge_store.py | 163 | Distilled patterns |
| `MemoryStore` | core/memory.py | 443 | Facade over 3-layer memory |
| `Scheduler` | core/scheduler.py | 165 | Rate limit enforcement |
| `VerificationTracker` | adapters/moltbook/verification.py | 236 | Challenge solver |
| `DomainConfig` | core/domain.py | — | @dataclass(frozen=True) |
| `ContentManager` | adapters/moltbook/content.py | 130 | Content generation |
| `AutonomyLevel` | adapters/moltbook/agent.py | — | Enum: APPROVE/GUARDED/AUTO |

## CLI Commands

```
contemplative-agent init [--config-dir PATH]
contemplative-agent register [--username U] [--password P]
contemplative-agent status
contemplative-agent run [--session M] [--approve|--guarded|--auto]
contemplative-agent distill [--days N] [--dry-run] [--no-axioms]
contemplative-agent distill-identity [--days N] [--dry-run]
contemplative-agent insight [--days N] [--dry-run]
contemplative-agent generate-report [--date YYYY-MM-DD] [--all]
contemplative-agent solve "TEXT"
contemplative-agent meditate [--days N] [--cycles N] [--dry-run]
contemplative-agent install-schedule [--interval H] [--session M] [--uninstall]

Global flags:
  --config-dir PATH           Override CONTEMPLATIVE_CONFIG_DIR
  --domain-config PATH        Custom domain.json
  --constitution-dir PATH    Custom constitution directory
  --no-axioms                 Disable CCAI constitutional clauses
  --approve / --guarded / --auto  Autonomy level
```

## Prompt Templates (17)

In `config/prompts/*.md`, lazy-loaded via `prompts.py`:
- system, relevance, comment, reply, cooperation_post, post_title
- topic_extraction, topic_novelty, topic_summary, session_insight
- submolt_selection, insight_extraction
- distill, distill_refine, identity_distill, identity_refine
- meditation_interpret

## Persistent State Files

| File | Format | Location | Purpose |
|------|--------|----------|---------|
| `credentials.json` | JSON (0600) | `MOLTBOOK_HOME` | API key + agent ID |
| `rate_state.json` | JSON (0600) | `MOLTBOOK_HOME` | POST/GET budgets, timestamps |
| `logs/YYYY-MM-DD.jsonl` | JSONL (0600) | `MOLTBOOK_HOME` | Daily episodes |
| `agents.json` | JSON (0600) | `MOLTBOOK_HOME` | Followed agents list |
| `commented_cache.json` | JSON (0600) | `MOLTBOOK_HOME` | Post dedup cache |
| `knowledge.json` | JSON | config/ | Learned patterns |
| `identity.md` | Markdown | config/ | Agent persona (2-stage distill) |
| `history/identity/` | Markdown | config/ | Identity archives (timestamped) |
| `skills/*.md` | Markdown | config/ | Behavior patterns (insight) |

## Security Boundaries

```
External Input              Validation
--------------              ----------
post_id                     VALID_ID_PATTERN ([A-Za-z0-9_-]+)
LLM output                  _sanitize_output() (FORBIDDEN_* + cap length)
Feed content                wrap_untrusted_content() + 1000 char cap
Knowledge context           wrap_untrusted_content()
Identity file               FORBIDDEN_SUBSTRING_PATTERNS + archive
domain.json / rules/*.md    FORBIDDEN_SUBSTRING_PATTERNS on raw content
HTTP redirects              allow_redirects=False
API domain                  ALLOWED_DOMAIN (www.moltbook.com only)
Ollama URL                  LOCALHOST_HOSTS + OLLAMA_TRUSTED_HOSTS
```

## Performance & Rate Limiting

**3-layer defense**:
1. Scheduler.has_read_budget() / has_write_budget() — proactive budget check
2. Adaptive waiting — sleep before hitting limits
3. 429 backoff — exponential retry (cap 300s per Retry-After)

**Budgets**: GET 60 req/min, POST 30 req/min (separate quotas, daily reset at UTC midnight)
